# generated by terraform

[services]
%{ for ip in service_ips ~}
${ip} ansible_ssh_user=${user} ansible_ssh_private_key_file=${private_key_path}
%{ endfor ~}

[elasticsearch]
%{ for ip in elasticsearch_ips ~}
${ip} ansible_ssh_user=${user} ansible_ssh_private_key_file=${private_key_path}
%{ endfor }